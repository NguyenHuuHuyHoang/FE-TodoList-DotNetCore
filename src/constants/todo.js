import { createActionTypes } from "src/ultils/createAsyncAction";

export const GET_TODOS = createActionTypes("GET_TODOS");
export const CREATE_TODO = createActionTypes("CREATE_TODO");
export const FETCH_STATUS = createActionTypes("FETCH_STATUS");
